# Execution peer review — pass 1

**Plan folder:** `docs/platform-base/`  
**Date:** 2026-09-19  
**Scope:** full (P0–P6)  
**Reviewer:** execution-peer-review skill  
**Baseline:** [PLATFORM_BASE_FINDINGS.md](../../PLATFORM_BASE_FINDINGS.md), [PLATFORM_BASE_GENERAL_PLAN.md](../../PLATFORM_BASE_GENERAL_PLAN.md), codebase paths cited below  

## Verdict

Review complete: 7 files, 0 critical, 2 high, 14 medium/low.  
**BLOCK phase-execution:** yes — P5.1 Files still enumerates dirs (Q8 is whole-tree), and P4.4 records the `saas-base-v2` SHA before the ship-gate commit/tag exists.

## Rollup (critical / high)

| Severity | Phase | Title |
|:---|:---|:---|
| high | P4 | SHA write vs tag/commit order |
| high | P5 | Files list reintroduces enumerated copy |

## Prior pass

None (first pass).

## Per-phase findings

## P0 — `docs/platform-base/PLATFORM_BASE_P0_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution contract | P0.0 embedded JSON `scope` is `backend/**` + `frontend/**` only. Current `.agent/review-context.json` (post architecture review) also has `deploy/**`. Re-running P0.0 as written would drop `deploy/**`. P0 is **landed** and general plan says do not re-open — LOOP starts at P1, so this is not a rework risk unless someone re-executes P0. |
| low | clarity | Goal still says “no push yet” / “Writing P1–P6 execution files” is Out. Accurate for when P0 ran; README already marks P0 done and P1–P6 written. |

**Verified OK:** (1) Header links findings + general plan; **Push:** `local`; Next → P1. (2) 6 subphases; P0.0 omits `rule_packs` (manifest `rule_packs` is null). (3) LOOP ship gate inlined with Bugbot REPEAT; step 6 do not push; no Revy drain. (4) Out lists P1–P6. (5) Tag SHA `48c361e20081bd71a4cbf176c9ba71bd09869082` matches peeled `saas-base-v1.1`. (6) Import forbids clone-over. No migration. No TBD.

## P1 — `docs/platform-base/PLATFORM_BASE_P1_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | findings | Q5 strips **LLM env**. P1.4 lists `github_*`, `revy_bot_login`, moonshot, voyage — not `anthropic_api_key` in `backend/app/core/config.py`. General plan P1 names moonshot/voyage only. Either add anthropic to P1.4 or explicitly park it for P3. |
| low | execution contract | P1.2 deliverable `--ignore=tests/unit/test_github_installations.py` while the same subphase **deletes** that file. Harmless; phase gate already runs full `pytest tests/unit/`. |

**Verified OK:** (1) Q6/Q11/Q13/Q14 encoded; P1 grep omits `.cursor/`; Pause LOOP on P1.1; full Alembic ids. (2) 5 subphases; Files match live consumers (`routerInstance`, `AppShellLayout`, dashboard hooks/checklist/quick-actions, settings test, `plan_gates`, enums, `models/__init__.py`). (3) `celery_app.py` stub routes `github_tasks.*` / `github_publish` exist (no modules) — listed. (4) FE + BE phase gate; **Push:** `first-push`; no Revy poll; do not drain comments. (5) Next → P2. No TBD.

## P2 — `docs/platform-base/PLATFORM_BASE_P2_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| low | Files | `frontend/src/contexts/AuthContext.test.tsx` is listed but **not in this tree** (comes from #32). Same for the five `test_keycloak_*.py` modules — they are in the Deliverable, not in P2.2 **Files**. Agent will still hit them via pytest; mark `(new)` on those paths. |

**Verified OK:** (1) Q6 full ids; Pause LOOP P2.1; vymalo called absent — Dockerfile has no JARs today. (2) Webhooks Stripe-only; `keycloak_provisioning.py` absent. (3) 4 subphases; BE+FE gate; **Push:** `local`; Next → P3. (4) Test inventory matches findings. No TBD. No GitHub webhook.

## P3 — `docs/platform-base/PLATFORM_BASE_P3_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | Files vs grep | P3.1 **Files** are `00-core.mdc`, `app-color-tokens.mdc`, `.cursorrules`. Deliverable greps all of `.cursor/` for `REVY_PRODUCT_SLICE` / `tokens.revy`. **Verified:** `.cursor/rules/git-quick.md` still cites `REVY_PRODUCT_SLICE.md`. Gate will fail until that file is edited — add it to Files. |
| medium | codebase | P3.2 Files name only `test_keycloak_issuer.py`. Other unit tests hardcode `revy-api` / `revy-web` (`test_auth_audience.py`, `test_decode_access_token.py`, `test_billing_config.py`) and are **not** in the P3 grep. Goal is leftover Revy product strings; list those tests or add client-id patterns. |
| low | phase gate | P3.4 grep includes `.cursor`, `AGENTS.md`, `README.md`; the phase-gate rg is `backend frontend deploy` only for brand strings (`.cursor` only for `tokens.revy` / GitHub installations). Align the gate with P3.4. |

**Verified OK:** (1) Q13/Q14 — rules in P3 not P1; no `tokens.app.css`; no Irbene names. (2) 4 subphases; `APP_REPLACE.md` named new; BE+FE gate; **Push:** `local`; Next → P4. (3) `.cursorrules` still mentions `tokens.revy.css` — listed. (4) `/tmp/revy` and `keycloak_realm="revy"` in P3.2. No TBD.

## P4 — `docs/platform-base/PLATFORM_BASE_P4_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| high | execution contract | P4.4 **What** writes the `saas-base-v2` SHA into findings/general plan **before** the ship-gate commit. Tag is ship-gate step 8 on **that** commit. The SHA does not exist yet during P4.4. As written the agent will record the wrong hash, amend (forbidden), or skip the write. Sequence: ship commit → tag that SHA → **then** record SHA (second commit is OK) or record in P5. |
| medium | human gate | P4.3 Mode A smoke needs a running local KC+Postgres. No **human/ops** pause. If bootstrap is not already up, LOOP will stall or skip smoke. Add a one-line operator gate or “fail the phase if stack is down” (do not skip). |
| low | closeout | Correctly does **not** clear `active_program` (P6). Template `batch` without Revy poll matches `integrations.revy: false`. |

**Verified OK:** (1) Q14 grep includes `.cursor/`; alembic `0011` full id; no droplet/Irbene. (2) 4 subphases; BE+FE gate; **Push:** `batch`; push includes P2–P3; Next → P5. (3) CONSUMER_COPY Out-list matches Q8. (4) No TBD.

## P5 — `docs/platform-base/PLATFORM_BASE_P5_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| high | findings / Files | Q8 / P5 **What** = whole tagged tree minus Out-list. P5.1 **Files** is still an enumerated dir list (`backend/`, `frontend/`, `deploy/`, …). LOOP agents copy **Files**. They will miss root/template files not listed (e.g. root README, `backend/docker-compose.yml` is under backend so OK, but anything unlisted at repo root). Make Files: “all paths on `saas-base-v2` except Out-list” with no dir menu. |
| medium | cwd | Header: runs in `irbene_gate`. Deliverables/phase gate use `../irbene_gate/...` (saas-base cwd). Ship gate is in the consumer. Pick one cwd and stick to it. |
| medium | gitignore | Consumer `.gitignore` is kept (verified: ignores `.env`, learning dirs; **no** `node_modules/`, frontend build). Template `.gitignore` is skipped. After copy, `npm install` artifacts are unignored. Say **merge** template ignore rules into the consumer file without dropping learning entries — still satisfies “do not clobber”. |
| low | assumption | “Currently not a git repo” — **verified** (`fatal: not a git repository`). `.env` exists and is ignored. Vision + VIRAC env example + learning Pipfile match the phase gate. |

**Verified OK:** (1) Q8 Out-list including `.gitignore` / `.env.example` / root Pipfile. (2) 3 subphases; **Push:** `first-push` on Irbene; no Revy; Next → P6. (3) No ontology. (4) Do not clone saas-base over the folder. No TBD.

## P6 — `docs/platform-base/PLATFORM_BASE_P6_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | deliverable | P6.1 `rg … \| head` is not a pass/fail assertion. Require a concrete string (e.g. realm or `VIRAC_*` present and `revy.createit.digital` absent in consumer env). |
| medium | human gate | P6.2 Mode A same as P4.3 — needs a running Irbene stack. No operator pause; easy to skip. |
| medium | closeout | Ship gate spans **two remotes** (consumer overlay + template SSOT). Explicit, but step 5 is one commit message for both. Split “commit in irbene_gate then commit in saas-base” so neither repo is left dirty. |
| low | cwd | Same `../irbene_gate` vs “runs in irbene_gate” mix as P5. Phase-gate `python3` on `.agent/review-context.json` assumes cwd is the **template**. |

**Verified OK:** (1) Q9 no ontology; vision not rewritten; no leak into the tag. (2) 4 subphases; last file **Push:** `batch`; `active_program` → `null`; gap table; Next none. (3) No Revy poll. (4) Consumer P6 gate files exist today (vision, no backend until P5). No TBD.

## Open questions

1. **P4:** After the ship-gate commit and tag, document the SHA in a follow-up docs commit on the same branch, or move the findings SHA line to P6.4?
2. **P5:** Confirm Files = every path on `saas-base-v2` except the Out-list (no dir menu). Merge template `.gitignore` rules into the consumer file instead of keeping consumer-only ignores?

