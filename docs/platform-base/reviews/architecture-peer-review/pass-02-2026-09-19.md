# Architecture peer review — pass 2

**Plan folder:** `docs/platform-base/`  
**Date:** 2026-09-19  
**Scope:** full re-read of findings + general plan after the pass-1 fix edit (not a thin delta-only skim). P0 execution still **not** reviewed.  
**Reviewer:** architecture-peer-review skill  
**Baseline:** [PLATFORM_BASE_FINDINGS.md](../../PLATFORM_BASE_FINDINGS.md), [PLATFORM_BASE_GENERAL_PLAN.md](../../PLATFORM_BASE_GENERAL_PLAN.md), codebase paths cited below  

## Verdict

Review complete: 0 critical, 1 high, 4 medium/low.  
**BLOCK create-execution-plan:** yes — P1/P4 product grep includes `.cursor/` for `tokens.revy`, but P1 Out forbids retargeting `.cursor/rules` (P3). P1 cannot green that gate as written.

## Prior pass

[pass-01-2026-09-19.md](./pass-01-2026-09-19.md). Fix agent locked Q11–Q13, full Alembic ids, P1 consumers + `npm test`, template vs consumer wording, push cadence, `deploy/**` on review-context, P2 test inventory, P3 grep extras.

## Rollup (critical / high)

| Severity | Section | Title |
|:---|:---|:---|
| high | P1 vs P3 | Product grep on `.cursor/` for `tokens.revy` is owned by P3, not P1 |

## Pass-1 item status

| Pass 1 | Status |
|:---|:---|
| P1 grep vs historical docs | **resolved** — Q11 delete corpus; application-tree grep; not unscoped `docs/` |
| P1 consumers + pytest-only | **resolved** — In-list + `npm test`; findings catalog matches grep |
| P3 `.cursor/rules` not in scope | **resolved** as P3 In — **new conflict** with P1/P4 same grep (below) |
| Q8 vs P5 dir list | **resolved** — Q8 = Q12 = whole tree minus Out-list |
| “This repo” = irbene_gate | **resolved** — template vs consumer terminology |
| Alembic shorthand | **resolved** — Q6 / P1 / P2 use full `revision` strings (verified against files) |
| P0 Next stale | **resolved** — P0 marked done; next is P1–P6 execution |
| review-context misses deploy | **resolved** — `deploy/**` in `.agent/review-context.json`; BUGBOT matches |
| Push cadence missing | **resolved** — general plan Push paragraph; no Revy poll |
| #32 test inventory | **resolved** — listed in findings + P2 deliverables |
| P3 grep misses `app_name` / `revy-keycloak` | **resolved** — deliverable grep expanded; i18n/fixtures in P3 In |
| P4 gate vs review-pipeline | **resolved** — Q11 delete + `test ! -d docs/review-pipeline` |
| P6 “this repo” | **resolved** |
| tokens.revy / `tokens.app.css` | **resolved** — Q13 fold into `tokens.css` |
| File count 462 vs 476 | **resolved** (called expected) |
| P1 sizing | **open, medium** — still tight; do not split unless execution Files explode |

## Per-section findings

## Cross-cutting

**Pn — no new gaps** on Q1–Q13 (no TBD). Homes, push cadence, and `active_program` match the tree.

| Severity | Area | Finding |
|:---|:---|:---|
| low | README | Program README P5–P6 Push column is still `—`. General plan already says consumer P5 `first-push`, P6 `batch`. `create-execution-plan` should copy the general-plan line into the README table, not invent a fourth kind. |

**Verified OK:** Q11–Q13 locked; Q6 full ids; terminology table; `.agent/review-context.json` scope includes `deploy/**`.

## P0 — Repo and prefix

**P0 — no new gaps** (verified: still done; do not write a second P0 execution file).

## P1 — Strip Revy product from the shell

| Severity | Area | Finding |
|:---|:---|:---|
| high | findings vs general plan | Findings: “Product grep (P1 and P4, **same**): … `backend/`, `frontend/`, `deploy/`, **`.cursor/`** for … `tokens.revy` must be empty.” P1 Out: “retargeting `.cursor/rules` (P3).” Q13: P3 retargets rules. **Verified now:** `.cursor/rules/00-core.mdc` and `app-color-tokens.mdc` contain `tokens.revy.css`. P1 cannot empty `.cursor/` without editing rules, which P1 must not do. Split the gate: P1 = `backend/` `frontend/` `deploy/` (+ corpus delete); P4 (after P3) adds `.cursor/`. **Do not** move all rule retarget into P1 — that undoes the Q13 split. |
| medium | corpus inbound links | Q11 deletes `docs/review-pipeline/`, `REVY_PRODUCT_SLICE.md`, `SCAFFOLD_P4_EXECUTION.md`. `docs/starter-pack/README.md` still calls those **Active** / P4 authority. Not covered by application-tree grep. P1 should retarget or drop those three inbound links (keep historical scaffold waves otherwise). |
| medium | sizing | In-list is large but still 5 buckets (Alembic; backend; FE+consumers; config/tokens/vector; corpus+grep). Fits 3–6 if execution does not split nav/dashboard into extra subphases. |

**Verified OK:** (1) Q6 parent/child ids match files: `0005` currently parents `0004`; target parent `2026_07_24_0200_0003_invitations`. (2) Live consumers still in tree as listed. (3) `--app-primary` already in `tokens.css`.

## P2 — Port user provisioning (#32)

**P2 — no new gaps** (verified: full Alembic ids; vymalo called out as absent; #32 test list matches `ed773f4` from pass 1).

## P3 — Generic platform identity

**P3 — no new gaps** on rules In-list and grep extras, except the P1/P4 grep ownership above.

| Severity | Area | Finding |
|:---|:---|:---|
| medium | grep holes | P3 grep still misses `export_storage_path` default `/tmp/revy/exports` and Python fixtures `keycloak_realm="revy"` (`KEYCLOAK_REALM=revy` is env-form). In-list already has fixtures and product name — execution must include those Files even if the grep string does not match. Optional: add `/tmp/revy` to the P3 deliverable grep. |

**Verified OK:** `00-core.mdc` / `app-color-tokens.mdc` still require installations + `tokens.revy.css` — correctly P3 work, once P1 grep no longer demands them empty.

## P4 — Prove and tag `saas-base-v2`

**P4 — no new gaps** once the product-grep split in P1 is locked (P4 = P1 tree grep **plus** `.cursor/` after P3). Frontend tests now in the P4 table. SHA calibration remains the only open item.

## P5 — Copy base into `irbene_gate`

**P5 — no new gaps** on whole-tree vs dir list (Q8 = Q12).

| Severity | Area | Finding |
|:---|:---|:---|
| medium | Out-list parity | Edge case still says do not overwrite **`.gitignore` learning entries**. Q8 Out-list names vision, `docs/platform-base/`, learning dirs, root `Pipfile` — not `.gitignore` or consumer `.env.example` (`VIRAC_*`). Add `.gitignore` (and consumer env example if it must survive) to the Q8 Out-list so P5 execution does not clobber them. |

## P6 — Irbene overlay (identity only)

**P6 — no new gaps** (verified: consumer wording; VIRAC only in consumer; Q9 still out).

## General-plan contract checklist

| Check | Result |
|:---|:---|
| Header links findings | yes |
| Locked table matches findings | yes (Q1–Q13) |
| Wave gate pytest / npm paths | yes — P1/P4 both; P2 tests named |
| Doc/corpus grep + exclusions | yes — application tree; Q11 deletes; **except** `.cursor/` phase ownership |
| Push strategy | yes — in general plan |
| No TBD in decisions | yes |

## Open questions

1. Product grep `.cursor/`: **omit until P4** (recommended; keeps Q13), or **allow P1 to edit only the `tokens.revy` rule lines** (splits rule work across P1 and P3)?

No other blocking questions. After (1) is a one-line lock in findings + P1/P4 deliverables, **BLOCK create-execution-plan** can clear.
