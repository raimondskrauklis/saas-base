# Architecture peer review — pass 1

**Plan folder:** `docs/platform-base/`  
**Date:** 2026-09-19  
**Scope:** full (findings + general plan). `PLATFORM_BASE_P0_EXECUTION.md` was attached by the user; **not reviewed** (use `execution-peer-review`).  
**Reviewer:** architecture-peer-review skill  
**Baseline:** [PLATFORM_BASE_FINDINGS.md](../../PLATFORM_BASE_FINDINGS.md), [PLATFORM_BASE_GENERAL_PLAN.md](../../PLATFORM_BASE_GENERAL_PLAN.md), codebase paths cited below  

## Verdict

Review complete: 0 critical, 6 high, 8 medium/low.  
**BLOCK create-execution-plan:** yes — P1 acceptance grep and P5 copy inventory are underspecified against the real tree; writing P1/P5 execution files now would guess glob exclusions and copy scope.

## Prior pass

None (first pass).

## Rollup (critical / high)

| Severity | Section | Title |
|:---|:---|:---|
| high | P1 | Grep/path gates disagree; historical Revy docs would fail a repo-wide content grep |
| high | P1 | Strip scope misses live consumers (dashboard, nav, plan_gates, i18n); ship gate is pytest-only |
| high | P3 | `.cursor/rules` still require GitHub installations + `tokens.revy.css`; not in P3 scope |
| high | P5 | Q8 “copy tagged tree” vs P5 enumerated dirs |
| high | findings | “This repo” still means `irbene_gate`; docs now live in `saas-base` |
| high | P1 / P2 | Alembic shorthand `0003`/`0009`/`0010` is not the real `revision` string |

## Per-section findings

## Cross-cutting — locked decisions, homes, workflow SSOT

| Severity | Area | Finding |
|:---|:---|:---|
| high | findings | § “Verified — this repo (`irbene_gate`)” and P6 gate “this repo” still speak as if the findings file lives in Irbene. Q2/Q3 locked canonical home as `saas-base`. An execution writer in this tree can misread “no app tree” or treat `docs/vision.md` as local (it is **not** in this repo). Disambiguate: **template repo** vs **consumer repo**. |
| medium | general plan | “Next: `create-execution-plan` for **P0**” is stale. P0 already landed (`c503168` on top of `48c361e`). Next is execution files for **P1–P6** (P5–P6 run in `../irbene_gate`). |
| medium | review-context | `.agent/review-context.json` `active_program` is `platform-base` with `backend/**` + `frontend/**`. P1–P3 also change `deploy/**` and Alembic; Bugbot scope will miss Keycloak image / nginx / `postgres-extensions.sql`. |
| medium | general plan | No push cadence in the general plan. Program README already has P1 `first-push`, P2–P3 `local`, P4 `batch`. Copy that into the general plan (or lock “follow README Push column”) so `create-execution-plan` does not invent a fourth kind or assume Revy poll (`manifest.integrations.revy` is `false`). |
| low | findings | File count 462 vs current `git ls-files` 476 — expected P0 docs/workflow overlay, not a shell-verification error. |
| low | findings | References still point at `revy/docs/...` and `kp-platform` skills. Fine as provenance; not a gate. |

**Verified OK:** (1) Q1–Q10 locked, no TBD. (2) General plan header links findings; P0–P4 here, P5–P6 in `../irbene_gate`. (3) `.agent/manifest.json` hosting GitHub, no Revy; `BUGBOT.md` already points at this program.

Calculator-track checklist (monolith/`_legacy`/stubs/CALC-*) — **N/A**.

## P0 — Repo and prefix

| Severity | Area | Finding |
|:---|:---|:---|
| low | general plan | Goal “history **ends** at `saas-base-v1.1`” is the import invariant, not the forever tip. After P0 the tip is the overlay commit; tag `saas-base-v1.1` still peels to `48c361e`. Fine if execution already encoded that — do not re-open P0 in a new execution file. |

**Verified OK:** (1) Tag `saas-base-v1.1^{}` = `48c361e` (annotated tag object `2889d405` is not a second commit). (2) History through the tag is present; only extra commit is P0. (3) Shell Alembic head is `2026_07_25_2340_0009_impersonation_sessions`. (4) `internal-docs/` not in git.

## P1 — Strip Revy product from the shell

| Severity | Area | Finding |
|:---|:---|:---|
| high | general plan vs codebase | P1 deliverable “grep gate for `github_installation` / `tokens.revy` is empty” is a **content** grep. Findings P4 gate is a **path** match on those strings. Content grep hits tracked Revy history that is not product code: `docs/starter-pack/REVY_PRODUCT_SLICE.md`, `SCAFFOLD_P4_EXECUTION.md`, `docs/review-pipeline/**` (those files shipped **on the v1.1 tag**, commit message even adds review-pipeline planning). Without glob exclusions **or** an explicit “delete vs keep-as-history” decision, P1 cannot green. |
| high | codebase | P1 In-list (API/FE/extensions + rename `registerRevy`) omits live consumers. Grep-verified leftovers that would 404 or fail Vitest after deleting `features/installations/`: `frontend/src/lib/routerInstance.tsx` (`/installations`), `AppShellLayout.tsx` nav, `checklistSteps.ts`, `QuickActionsWidget.tsx`, `dashboard/hooks.ts` (`fetchInstallations`), `IntegrationsSettingsPage.test.tsx`, `SetupChecklistWidget.test.tsx`. Backend: `plan_gates.py` `installations.create` + entire `test_plan_gates.py`; `enums.py` `GitHubAccountType` / `GitHubInstallationStatus`; `models/__init__.py`. P1 ship gate is only `pytest tests/unit/` — frontend tests are not a deliverable, so broken nav can ship. |
| high | findings / general plan | Q6 / P1 say `0005.down_revision = 0003`. Real ids: `revision = "2026_07_25_2217_0005_user_locale_timezone"`, target parent `revision = "2026_07_24_0200_0003_invitations"` (today `down_revision` is `2026_07_24_0300_0004_github_installations`). Literal `"0003"` / `"0004"` will not form a chain. Lock **full** `revision` / `down_revision` strings in findings before execution. |
| medium | general plan | `tokens.revy.css` is brand overrides imported from `frontend/src/index.css`. Deleting it for the P1 grep is correct **if** `--app-primary` stays in `tokens.css` (it already does). Rules still say “brand overrides in `tokens.revy.css` only” — that is a P3 rules fix, but P1 must not leave a dangling `@import`. |
| medium | sizing | P1 can fit 3–6 subphases only if dashboard/nav/plan_gates/i18n/docs-policy fold into the FE/BE buckets. If the execution Files list explodes, split P1 in the general plan rather than stuffing 8 subphases. |

**Verified OK:** (1) Product mixed in as claimed: `0004` migration, `backend/app/api/v1/workspaces/installations.py`, `registerRevy.ts` (GitHub card + installations widget + `PlanSummaryWidget`). (2) `stripe_enabled: bool = False` — keep. (3) `webhooks/__init__.py` is Stripe-only (no GitHub export at this tag). (4) `deploy/sql/postgres-extensions.sql` has `vector`. (5) `config.py` has `github_*` / `revy_*` / moonshot / voyage. (6) No `frontend/.../reviewer` UI (findings claim holds).

## P2 — Port user provisioning (#32)

| Severity | Area | Finding |
|:---|:---|:---|
| high | findings / general plan | Same Alembic shorthand: new `0010`+`0011` after `0009`. Real parent is `2026_07_25_2340_0009_impersonation_sessions`. #32 files are `2026_07_26_1900_0019_keycloak_webhook_deliveries` parented on **review** `0018_github_pull_request_is_draft`. Execution must rewrite ids **and** `down_revision`; keeping `0019`/`0020` is correctly Out. |
| medium | findings | “Same #32 tests adapted” is not inventoried. Verified on `revy` `ed773f4`: `test_keycloak_provisioning.py`, `test_keycloak_webhook.py`, `test_keycloak_webhook_verify.py`, `test_keycloak_webhooks.py`, `test_bootstrap_config.py`, plus edits to `test_auth_impersonation.py` and `AuthContext.test.tsx`. Paste that list into findings so P2 execution does not drop webhook-verify tests. |
| medium | codebase | Current KC image `deploy/keycloak/config/Dockerfile` has **no** vymalo JARs (plain `quay.io/keycloak/keycloak:26.0`). `revy` HEAD Dockerfile pins `0.10.0-rc.1`. P2 In-list is correct; do not treat vymalo as already present. |
| low | overlap | Findings overlap list matches: shell `ensure_user_from_token` / `maybe_auto_provision_user` vs #32 `provision_user_from_keycloak()`. Conflict-toward-#32 is locked. |

**Verified OK:** (1) `ed773f4` has no GitHub/review paths (findings). (2) No `provision_user_from_keycloak` in this tree. (3) Webhook router Stripe-only. (4) P2 Out “GitHub webhook module” is a no-op at v1.1. (5) P2 vs P4: P2 proves webhook-can-insert; P4 Mode A allows webhook **and/or** JIT — compatible.

## P3 — Generic platform identity

| Severity | Area | Finding |
|:---|:---|:---|
| high | general plan | P3 In-list is AGENTS/README/env/i18n/docker-nginx/extension ids. It does **not** include `.cursor/rules`. `00-core.mdc` still: “Revy domain — GitHub installations…” and “brand overrides in `tokens.revy.css` only”. After P1, agents following always-on rules will try to put product back. Same for `app-color-tokens.mdc`. P3 must retarget rules to generic SaaS (workspaces/settings/billing, not installations). |
| medium | general plan | P3 grep (`revy.createit.digital` / `revy[bot]` / `KEYCLOAK_REALM=revy`) misses other brand leaks already in tree: `app_name: str = "Revy"`, `export_storage_path = "/tmp/revy/exports"`, `image: revy-keycloak:26.0`, i18n “Deactivate your Revy account”, test fixtures `keycloak_realm="revy"`. “Placeholder-only” helps for realm, not for `app_name`. Expand the replace list **or** the grep. |
| low | split | Extension id `revy-github` dies in P1 with the GitHub card; P3 should only rename leftover `registerRevy` / `registerRevyExtensions` if P1 did not. Plan already says rename in P1 — keep that single-owner. |

**Verified OK:** (1) FE `common.appName` is already generic “App” / “Lietotne”; backend `app_name` is still `"Revy"`. (2) `frontend/.env.example` `VITE_KEYCLOAK_REALM=revy`. (3) `deploy/nginx/` and Keycloak README use `revy.createit.digital` / `/mnt/revy_volume`.

## P4 — Prove and tag `saas-base-v2`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | findings vs P1 | P4 path-match gate will **not** fail on `docs/review-pipeline/**` or `REVY_PRODUCT_SLICE.md` (those paths lack `github_installation` / `features/installations` / `tokens.revy`). If the product decision is “template must not ship Revy review plans”, P4’s gate is too weak; fix in P1 doc policy, then add a P4 path or content check. |
| low | general plan | Mode A smoke needs a running local KC+Postgres. That is an execution/env concern, not a phase-order bug. Keep “new DBs, not Revy staging” as written. |

**Verified OK:** (1) Findings P4 table (pytest, grep, alembic heads `0011`, Mode A `/me` active) matches the goal. (2) Tag home `raimondskrauklis/saas-base` matches Q2/Q10. (3) Calibration open item (write SHA back) is appropriate.

## P5 — Copy base into `irbene_gate`

| Severity | Area | Finding |
|:---|:---|:---|
| high | findings vs general plan | Q8 locked: copy the **tagged `saas-base-v2` tree**. P5 In-list is only `backend/`, `frontend/`, `deploy/`, agent/cursor rails, app env examples. Missing vs a runnable clone: `docs/starter-pack/`, `docs/saas-base/`, `docs/agents/`, `.github/`, compose/Makefiles, root template README. Selective copy **or** whole-tree-with-exclusions must be locked — not both. Exclusions in P5 Out (`docs/vision.md`, `docs/platform-base/`, learning dirs, root learning `Pipfile`) only make sense for a tree copy. |

**Verified OK:** (1) Out-list matches findings edge case (do not eat vision / learning / root Pipfile). (2) Consumer is not a submodule (Q8). (3) `docs/vision.md` is correctly **not** in `saas-base`.

## P6 — Irbene overlay (identity only)

| Severity | Area | Finding |
|:---|:---|:---|
| medium | findings | P6 gate is correct for the **consumer**, but wording “this repo” will be read inside `saas-base` program docs. Rename to `irbene_gate` everywhere. |
| low | general plan | Overlay `VIRAC_*` / Irbene realm belongs only in the consumer. Do not leak those names into the tagged template (P3 Out already says no Irbene brand). |

**Verified OK:** (1) Q9 ontology stays out. (2) Depends on P5. (3) Vision.md remains north star **in the consumer**.

## General-plan contract checklist

| Check | Result |
|:---|:---|
| Header links findings | yes |
| Locked table matches findings | yes (by reference Q1–Q10; no extra options) |
| P0.0 before code — `active_program`, review-context, BUGBOT | already present in this tree; not a phase in the general plan (OK given P0 done) |
| P0 `_legacy` / stub indicators / calculator test floor | N/A |
| Wave gate pytest paths exist or named **new** | P1 uses existing `tests/unit/`; P2 tests named **new** (adapt from #32) — inventory missing, see P2 |
| P6 doc/corpus grep gate with glob exclusions | **missing** — P1/P4 greps have no exclusions |
| Push strategy + batch phases explicit | only in program README, not general plan |
| No TBD / no open options in decisions | yes; open item is P4 SHA calibration only |

## Codebase verification (shell claims)

| Claim | Result |
|:---|:---|
| Alembic 0004 exists; 0005 parents 0004 | **verified** |
| Auth #32 not in tree | **verified** (no provisioning service / KC webhook) |
| `registerRevy.ts` = installations + plan widget (no reviewer widgets) | **verified** |
| Brand leak in config + nginx + `tokens.revy.css` | **verified** |
| `vector` in `postgres-extensions.sql` | **verified** |
| Stripe default off | **verified** |
| Findings “this repo = irbene_gate, no backend” | **stale relative to this file’s home**; not verified against `../irbene_gate` in this pass |
| `saas-base-v1.1` = `48c361e` | **verified** (peeled tag) |

## Open questions

1. Historical Revy corpus on the v1.1 tag (`docs/review-pipeline/`, `docs/starter-pack/REVY_PRODUCT_SLICE.md`, scaffold P4): **delete**, **keep with grep exclusions**, or **move to `internal-docs`** (not in git)? This unblocks P1/P4 gates.
2. P5 consume path: **whole `saas-base-v2` tree minus Out-list**, or **enumerated dirs**? Pick one in Q8/P5.
3. Generic theme after deleting `tokens.revy.css`: fold remaining brand map into `tokens.css`, or add `tokens.app.css` and retarget cursor rules in P3?

Do not start `create-execution-plan` until (1) and (2) are locked in findings + general plan, Alembic **full** revision strings are in Q6, P1 In-list includes dashboard/nav/plan_gates/i18n + frontend test gate, and P3 In-list includes `.cursor/rules`.
