# Architecture peer review — pass 3

**Plan folder:** `docs/mini-saas/`  
**Date:** 2026-09-20  
**Scope:** full (findings + general plan)  
**Reviewer:** architecture-peer-review skill  
**Baseline:** [MINI_SAAS_FINDINGS.md](../../MINI_SAAS_FINDINGS.md), [MINI_SAAS_GENERAL_PLAN.md](../../MINI_SAAS_GENERAL_PLAN.md), codebase paths cited below

## Verdict

Review complete: 0 critical, 3 high, 7 medium/low.  
**BLOCK create-execution-plan:** yes — dual-write order is contradictory, and two catalog items (password-reset API, dashboard KPI card) have no phase owner.

## Prior pass

[pass-02-2026-09-20.md](./pass-02-2026-09-20.md) reviewed findings only (1 critical / 3 high, **BLOCK create-general-plan: yes**). Fix agent then locked Q17–Q21, wrote the general plan, and updated operator runbooks (checklist, `APP_REPLACE` step 5, `KEYCLOAK_SETUP`, `MAILGUN_SETUP`).

| Pass 2 item | Status |
|:---|:---|
| critical — reject + KC disable echo clobbers `rejected` | **resolved** in Q17 + P0 (code still clobbers until P0 ships — expected) |
| high — execute-actions SMTP + `client_id`/`redirect_uri` | **resolved** in Q15; runbooks already document both; live SMTP is Q20 |
| high — realm-as-code vs clicks | **resolved** as Q20 lock (JSON + live clicks = `irbene_gate` after `saas-base-v3`, not this LOOP) |
| high — clone follow-ons (files / API keys / webhooks) | **resolved** as Q18/Q19 defer, out of this LOOP |
| medium — last-SA guard must count **active** only | **resolved** in Q5 |
| medium — dual-write order IdP vs DB | **open / escalated to high** (general plan still silent; `KEYCLOAK_SETUP` contradicts findings) |
| low — `GET /pending` drop vs alias | **open** (still two sentences; not blocking) |

Fix agent: edit **findings + general plan** per this file. Do not start `create-execution-plan` until BLOCK is no.

## Rollup (critical / high)

| Severity | Section | Title |
|:---|:---|:---|
| high | P3 / findings | Dual-write order contradicts (KP IdP-first vs runbook Postgres-first) |
| high | P3 / P4 | Password-reset API has helper + FE but no backend phase |
| high | P2 / P4 | `users_suspended` KPI card has no FE phase |

## P0 — Shared infra

| Severity | Area | Finding |
|:---|:---|:---|
| medium | general plan | P0 unit-tests the interceptor + Q17 machine, but `WEBHOOK_EVENTS_TAKEN` in `deploy/keycloak/config/.env.example` is still user events only (`REGISTER`, `IDENTITY_PROVIDER_*`, `UPDATE_PROFILE`, `UPDATE_EMAIL`, `DELETE_ACCOUNT`). `apply_keycloak_user_disabled` is only reached from `keycloak_webhooks.py:248–250` on `ADMIN*`. Without adding those events (or locking “inbound is latent until Q20”), Q17 never runs in any environment this LOOP touches. `KEYCLOAK_SETUP.md` §9 already flags this. One sentence in P0 In or P6 runbook pointer is enough. |
| medium | process | No P0.0. `.revy/review-context.json` and `.cursor/BUGBOT.md` still `platform-base`. Execution must add program `mini-saas` with `backend/**` + `frontend/**` (P0 touches `api.ts`). Name the slug in the general plan so `create-execution-plan` does not invent it. |
| low | general plan | P0 Out correctly rejects weakening `get_current_user` and `GET /me` 200-with-status (parking lot). |

**Verified OK:** (1) Interceptor hole is real — `api.ts:92` maps only `workspace_suspended`; JSON uses `"error": exc.error_code` (`exception_handlers.py:28`). (2) `/me` 403s suspended actors (`auth.py:219`) so `ProtectedRoute.tsx:41` never sees `status === 'suspended'` (`AuthContext.tsx` catch → `user=null`). Route `/account-suspended` exists (`routerInstance.tsx:60`). (3) `apply_keycloak_user_disabled` (`keycloak_provisioning.py:201`) still sets **any** status to `suspended` on `enabled=False` (`:213–214`); enable is already `suspended`→`active` only. Q17 + P0 is the right first phase. (4) JIT will not auto-activate suspended (`onboarding.py:80–85`).

## P1 — Outbound Keycloak Admin helper

| Severity | Area | Finding |
|:---|:---|:---|
| — | codebase | **Verified OK.** No Admin REST client in `backend/app` (grep `KEYCLOAK_ADMIN` / `/admin/realms` / `execute-actions`: none). Credentials exist: `keycloak_client_id` + `keycloak_client_secret` (`config.py:113–114`). `httpx` already used (`jwks.py`, mailgun). |
| medium | general plan | Existing outbound pattern is `backend/app/services/integrations/` (comment cites `OUTBOUND_HTTP.md`). P1 should say the helper lives there (or an equivalent app service), fail-closed, no `None`/`False` swallow — so execution does not invent a one-off next to `keycloak_provisioning.py`. |
| low | findings | Findings table still says the checklist does **not** require Service accounts / `realm-management`. **Stale.** `KEYCLOAK_DEV_CHECKLIST.md:36–37` and `APP_REPLACE.md` step 5 already require Service accounts ON + `manage-users` / `query-users` / `view-users`. P1 “runbooks already committed stay the operator SSOT” is the accurate line; findings item I / “verified — not configured” should be rewritten as “runbooks updated; live apply is Q20.” |

**Verified OK:** Q15 `client_id=app-web` + SPA `redirect_uri` is in P1 In. Live realm role assignment and SMTP are Out (Q20). Helper unused by product paths until P3 matches “no product path yet except tests.”

## P2 — Directory and detail APIs

| Severity | Area | Finding |
|:---|:---|:---|
| — | codebase | **Verified OK.** Copy target: `GET /admin/workspaces` + `?status=` + `?search=` + `order_by(created_at.desc(), id.desc())` (`admin_workspaces.py:44`, order `:95`). Queue today: `GET /pending` only, `created_at ASC`, no cursor (`users.py:229`). `AdminKpisResponse` has `users_pending_approval`, no `users_suspended` (`schemas/admin.py:36–42`). Impersonation eligibility already matches W7 (`impersonation.py:65–74`: not SA, must be `active`). |
| high | general plan | Q11 / catalog H is a **dashboard card**, not only a schema field. P2 Out = FE; P4 is `/admin/users` only. No phase adds `users_suspended` on `AdminDashboardPage.tsx` (today `:65–68` is pending-approval only). Lock: schema in P2, card + EN/LV in P4 (or a one-line P2 exception). |
| low | general plan | `GET /pending` keep-until-P4 then “drop or unused-alias” is still two end states. FastAPI: keep `/pending` registered before any untyped `/{user_id}`; UUID-constrained `/{id}` is safe. Execution picks one. |

**Verified OK:** P2 depends on P0 not P1 — list/detail/KPI do not need the Admin helper. Default omit `deleted` matches Q12. No email/password fields (Q6).

## P3 — Lifecycle mutations

| Severity | Area | Finding |
|:---|:---|:---|
| high | findings vs runbook vs plan | Dual-write order is **three different sentences**: findings research “Adopt intent” of KP (`KC enabled`, then DB commit, restore KC if commit fails); `KEYCLOAK_SETUP.md:232` “App outbound suspend still writes Postgres first (fail closed)”; general plan P3 “fail closed (no app-only status)” + “Admin failure rolls back” (covers KC fail → DB rollback only, not commit fail → KC restore). Execution will guess. **Recommended lock:** mutate the row in the open transaction → helper (raise on HTTP/role errors) → `commit`; if commit fails after a successful Admin call, restore KC `enabled` (and do not treat logout as undoable). That is Postgres-first in the session, KP restore on the second failure, and “no app-only status.” Write it once in findings + P3. |
| high | general plan | Catalog F / Q15 password-reset is `PUT …/execute-actions-email` `UPDATE_PASSWORD` from **user detail**. P1 builds the helper. P4 is the button. **No phase adds the backend route** (e.g. `POST /api/v1/admin/users/{id}/send-password-reset`). P3 In is only approve/reject/suspend/reactivate. Add that mutation to P3 (same fail-closed helper + audit if findings want one). |
| medium | general plan | VERIFY_EMAIL on approve is fail-closed (Q15) while live SMTP is Out (P3 / Q20). Correct for mocked tests; Mode B against a stock local realm will 4xx if `emailVerified` is false. MAILGUN_SETUP already says accept 4xx until SMTP exists. P3 tests must mock execute-actions; do not put live SMTP in the P3 gate. |
| low | general plan | P3 is the fattest phase (approve + VERIFY_EMAIL, reject + logout, suspend + guards + logout, reactivate, password-reset if added, four audit names). Confirm it still maps to 3–6 execution subphases; split P3a/P3b if not. |

**Verified OK:** Approve/reject already `with_for_update()` (`users.py:241`, `:255`) and still skip `record_audit` (contrast `admin_workspaces.py:177` `platform.workspace.suspended`). Self-suspend lockout is real (`auth.py:219` 403s SA). Q5 last **active** SA + Q13 reactivate `suspended`→`active` only are in P3. No promote-`super_admin`. No Alembic — `UserStatus.suspended` already exists (`enums.py:35`).

## P4 — Admin users UI

| Severity | Area | Finding |
|:---|:---|:---|
| high | general plan | See P2: dashboard `users_suspended` card is not in P4 In. See P3: password-reset FE assumes an API that P3 does not list. |
| — | codebase | **Verified OK.** `AdminUsersPage.tsx` is queue-only (`fetchPendingUsers`). Copy target `WorkspacesPage.tsx` already has status filter + search + load-more. No `/admin/users/:userId` route (`routes.tsx:27`). |

**Verified OK:** EN+LV + `--app-*` are cross-cutting. Approve/reject stay on `pending_approval` rows. Out: in-app password/email edit; merge with `/settings/team`.

## P5 — Revy review / P6 — Closeout

| Severity | Area | Finding |
|:---|:---|:---|
| — | general plan | **Verified OK.** Hosting is GitHub + Revy (`.agent/manifest.json` `hosting.kind: github`). Push cadence P0 `local` → P1 `first-push` → P2–P3 `local` → P4–P6 `batch` after Revy idle matches the LOOP contract. P6 tag **`saas-base-v3`** on `main`, not a PR branch, not a retag of `v2` (Q21). SHA is the only open calibration. irbene_gate copy / realm JSON / live clicks correctly **Not this LOOP**. |
| medium | findings | W5 “no in-app user directory” is still live in `SAAS_BASE_W5_EXECUTION.md:14`, `SAAS_BASE_W5_PLATFORM_GENERAL_PLAN.md:15`, `SAAS_BASE_FINDINGS.md:150`. P6 In lists this — good. Do not treat it as already done. |
| low | process | P1 first-push of a helper-only tip is fine if P0 commits ride along; P2–P6 then batch after that Revy cycle idles. |

## Cross-cutting

| Check | Result |
|:---|:---|
| Header links findings | yes |
| Locked table matches findings (Q1–Q21, no TBD) | yes — Q18–Q21 deferred extras are out of LOOP |
| P0.0 `active_program` / review-context / BUGBOT | **missing** from general plan; still `platform-base` |
| Wave-gate pytest paths | unnamed **new** (OK for general plan; execution names `tests/unit/` mocks) |
| Push strategy + batch phases | yes |
| No TBD in decisions | yes; open item is SHA calibration only |
| `require_super_admin()` + `require_impersonation_allowed()` | yes (Q3; current router already both) |
| Fail closed / no new columns / no KC create-delete / no Alembic | yes |
| i18n EN+LV / `--app-*` | yes |

**Verified OK:** Q17 in findings matches P0. Q16 runbooks-this-program + live-apply-Q20 matches general-plan home paragraph. Catalog C/D audits `platform.user.*` are named. Spaces / API keys / Playwright / extra roles stay out.

## Findings accuracy (vs repo, 2026-09-20)

| Claim | Verdict |
|:---|:---|
| Queue-only API/FE | **true** — `admin/users.py` pending + approve/reject only; `AdminUsersPage` `fetchPendingUsers` |
| Approve/reject skip `record_audit` | **true** |
| No outbound Admin client | **true** |
| Checklist does not require service-account roles | **false / stale** — checklist + APP_REPLACE step 5 already require them |
| Inbound disable always suspends | **true** until P0 |
| `users_suspended` KPI absent | **true** |
| Victim interceptor hole | **true** |
| Workspace list contract | **true** (`created_at DESC, id DESC`) |
| DB snapshot `active=1` | snapshot only — not a gate (unchanged from pass 1) |

## Suggested edits (fix agent)

1. **Findings + P3:** one locked dual-write sentence (recommended: uncommitted row → helper raise → commit; restore KC `enabled` if commit fails after Admin success). Align or delete `KEYCLOAK_SETUP.md:232` “Postgres first” so it does not fight the plan.
2. **P3 In:** `POST` (name it) send-password-reset via execute-actions `UPDATE_PASSWORD`; fail closed; tests mock helper.
3. **P2 / P4:** `users_suspended` on `AdminKpisResponse` in P2; dashboard card + i18n in P4 In.
4. **Findings:** replace “checklist does not require Service accounts” with “runbooks already list the three roles; live apply is Q20.”
5. **General plan header / P0:** `active_program` slug `mini-saas`; P0.0 in execution. Optional: ADMIN\* events in `WEBHOOK_EVENTS_TAKEN` as P0/P6 runbook line.
6. Do not reopen Q17–Q21. Do not add realm JSON or irbene_gate copy to this LOOP.

## Open questions

None blocking beyond the locks above — fix agent can apply the recommended dual-write sentence without a product decision.

## General-plan contract checklist

| Check | |
|:---|:---|
| Header links findings | yes |
| Locked table matches findings | yes (after dual-write sentence) |
| P0.0 before code | **no** — add slug; execution writes the JSON |
| Wave gate pytest paths exist or named **new** | named new (implied) |
| Push strategy + batch push phases explicit | yes |
| No TBD / no open options in decisions table | yes, except dual-write (this pass) |
