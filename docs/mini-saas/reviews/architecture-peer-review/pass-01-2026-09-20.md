# Architecture peer review — pass 1

**Plan folder:** `docs/mini-saas/`  
**Date:** 2026-09-20  
**Scope:** full (findings only; no general plan)  
**Reviewer:** architecture-peer-review skill  
**Baseline:** [MINI_SAAS_FINDINGS.md](../../MINI_SAAS_FINDINGS.md)

## Verdict

Review complete: 0 critical, 0 high, 3 medium/low.  
**BLOCK create-general-plan:** no — claims match the repo; Phase-0 victim UX is correctly required.  
**BLOCK create-execution-plan:** n/a — no general plan yet.

## Prior pass

None.

## Rollup (critical / high)

| Severity | Section | Title |
|:---|:---|:---|
| — | — | none |

## What exists vs genuinely new

| Severity | Area | Finding |
|:---|:---|:---|
| — | codebase | **Verified OK.** Queue-only API/FE: `admin/users.py` has `GET /pending` + approve/reject only. `AdminUsersPage.tsx` calls `fetchPendingUsers` only. `list_pending_users` has no cursor. `AdminKpisResponse` has no `users_suspended`. W5 “no in-app user directory” still in `SAAS_BASE_W5_EXECUTION.md`. |
| medium | findings | Citation for workspace-suspend audit was corrected to `admin_workspaces.py:177` during this pass (was `:142` = `platform.workspace.viewed`). No remaining stale line. |

**Verified OK:** inbound `apply_keycloak_user_disabled` exists; no outbound Keycloak Admin client in `backend/app`.

## Catalog — users page

| Severity | Area | Finding |
|:---|:---|:---|
| — | codebase | **Verified OK.** Workspace list contract to copy: `GET /admin/workspaces` + `?status=` + `?search=` + `order_by(created_at.desc(), id.desc())` (`admin_workspaces.py:95`). Impersonation target rules match Q7 (`impersonation.py:65–74`). Self-suspend lockout is real: `get_current_user` 403s suspended actors including SA (`auth.py:219`). |
| low | findings | `GET /pending` keep-then-drop vs keep-as-alias is two sentences in catalog A; Q4 already locks “keep until FE uses the new list.” General plan should pick one end state. |

## Catalog — mini extras

| Severity | Area | Finding |
|:---|:---|:---|
| — | findings | **Verified OK.** Defer/reject lists do not smuggle KP institutions or a Keycloak Admin clone. Billing stays Stripe Dashboard (W5). Registration flags are GET-only (`admin/settings.py`). |

## Edge cases / Q1 KC enable undoes app suspend

| Severity | Area | Finding |
|:---|:---|:---|
| — | codebase | **Verified OK.** `apply_keycloak_user_disabled` `enabled=True` sets `suspended`→`active` (`keycloak_provisioning.py:210`). JIT will not auto-activate suspended (`onboarding.py:80`). |

## Phase-0 victim UX

| Severity | Area | Finding |
|:---|:---|:---|
| — | codebase | **Verified OK.** JSON body uses `"error": exc.error_code` (`exception_handlers.py:28`), so `api.ts` `body?.error === 'workspace_suspended'` is the right mirror for `account_suspended`. `ProtectedRoute` never sees `user.status === 'suspended'` today because `/me` 403s first (`AuthContext.tsx` catch → `user=null`). |

## Decisions registry

| Severity | Area | Finding |
|:---|:---|:---|
| low | findings | Thirteen Qs all **locked**. That is appropriate after prior chat + this investigation; general plan must not reopen Q1/Q2 without an explicit findings edit. |
| low | process | No `active_program` switch requested; `.agent/review-context.json` still `platform-base` (idle/null expected after P6). Out of scope for this pass. |

## General-plan contract checklist

Not applicable — no general plan in this folder.

## Open questions

None blocking. Optional for general plan only: end state of `GET /pending` (delete vs unused alias).

## Cross-cutting

- Locked table does not contradict W5 workspace/audit/impersonation reuse.
- DB check in findings (`active=1` on `saas_base_dev`) is a snapshot, not a test fixture — general plan should not treat that count as a gate.
