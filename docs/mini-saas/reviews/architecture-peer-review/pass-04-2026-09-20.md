# Architecture peer review — pass 4

**Plan folder:** `docs/mini-saas/`  
**Date:** 2026-09-20  
**Scope:** delta (pass-3 highs + applied mediums; Q17–Q21 unchanged)  
**Reviewer:** architecture-peer-review skill  
**Baseline:** [MINI_SAAS_FINDINGS.md](../../MINI_SAAS_FINDINGS.md), [MINI_SAAS_GENERAL_PLAN.md](../../MINI_SAAS_GENERAL_PLAN.md), [KEYCLOAK_SETUP.md](../../../utils/KEYCLOAK_SETUP.md) §9

## Verdict

Review complete: 0 critical, 0 high, 1 low.  
**BLOCK create-execution-plan:** no — pass-3 highs are locked and aligned across findings, general plan, and KEYCLOAK_SETUP §9.

## Prior pass

[pass-03-2026-09-20.md](./pass-03-2026-09-20.md) (0 critical / 3 high, **BLOCK create-execution-plan: yes**). Fix agent locked Q22, named the password-reset route on P3, split the KPI schema/card across P2/P4, and applied the pass-3 mediums (runbooks already list roles; helper path; `mini-saas` slug; inbound `ADMIN*` latent; drop `GET /pending`). Q17–Q21 unchanged. P3 stays one phase.

## Rollup (critical / high)

| Severity | Section | Title |
|:---|:---|:---|
| — | — | none |

## Pass-3 items

| Pass 3 item | Status |
|:---|:---|
| high — dual-write order contradicts | **resolved** — Q22 + catalog D + research table + P3 + `KEYCLOAK_SETUP.md` §9 are the same sentence. KP IdP-first is not adopted; restore of `enabled` on commit-fail is. Logout not undoable. |
| high — password-reset API has no backend phase | **resolved** — P3 owns `POST /api/v1/admin/users/{id}/send-password-reset`; Q15 + catalog F name the same path; P4 is the button; tests mock; live SMTP Q20. |
| high — `users_suspended` KPI card has no FE phase | **resolved** — P2 schema on `AdminKpisResponse` (Out FE); P4 dashboard card + EN/LV. Q11 + catalog H match. |
| medium — `ADMIN*` / `WEBHOOK_EVENTS_TAKEN` | **resolved** — P0 Out + P6 pointer; inbound latent until Q20. |
| medium — `active_program` slug | **resolved** — general plan names `mini-saas`; P0.0 in execution. JSON still `platform-base` until P0.0 (expected). |
| medium — helper location | **resolved** — P1 In `backend/app/services/integrations/`. |
| medium — stale “checklist does not require roles” | **resolved** — findings verified row now “runbooks already require… live apply Q20.” Checklist + APP_REPLACE step 5 still list the three roles. |
| medium — VERIFY_EMAIL vs no live SMTP | **resolved** — P3 Out: tests mock; MAILGUN 4xx until Q20. |
| low — `GET /pending` drop vs alias | **resolved** — Q4 + catalog A + P2/P4: keep until P4, then drop. |
| low — P3 phase size | **open, not blocking** — still one phase as requested; `execution-peer-review` checks 3–6 subphases. |

## P0 — Shared infra

**P0 — no new gaps** (verified: Q17 machine + interceptor In; `ADMIN*` Out / latent; P0.0 slug named).

## P1 — Outbound Keycloak Admin helper

**P1 — no new gaps** (verified: `integrations/` path exists; no Admin client in `backend/app` yet; runbooks SSOT; live realm Out).

## P2 — Directory and detail APIs

**P2 — no new gaps** (verified: KPI field In, dashboard card Out → P4; `/pending` kept until P4).

## P3 — Lifecycle mutations

| Severity | Area | Finding |
|:---|:---|:---|
| — | general plan | **Verified OK.** Q22 in P3 In; send-password-reset route named; commit-fail restore in deliverables; live SMTP Out. |

**Verified OK:** Password-reset is Admin-only (no status dual-write) in Q22 + catalog F. Approve/reject/suspend still Q22 (status row). Audits named for the four status mutations; password-reset has none (acceptable — findings never required one).

## P4 — Admin users UI

**P4 — no new gaps** (verified: KPI card + password-reset button + drop `/pending`).

## P5 / P6

**P5–P6 — no new gaps** (verified: Q1–Q22 in P5 deliverable; W5 “no directory” still P6; `ADMIN*` pointer; tag on `main`).

## Cross-cutting / leftovers

| Severity | Area | Finding |
|:---|:---|:---|
| low | findings | “Genuinely new” still says apply + smoke GET on **this product’s** Keycloak (`/admin/realms/app/users/{id}`). Catalog I, Q16/Q20, and the general-plan home paragraph already lock live apply + smoke on **irbene_gate**. Execution follows Q20; rewrite that bullet in a docs pass if it still bothers you — not a phase hole. |

**Verified OK:** Q1–Q21 resolutions unchanged. Decisions table has no TBD. Open item remains v3 SHA calibration.

## Open questions

None.

## General-plan contract checklist

| Check | |
|:---|:---|
| Header links findings | yes |
| Locked table matches findings (Q1–Q22) | yes |
| P0.0 before code — slug named | yes (`mini-saas`); JSON in execution |
| Wave gate pytest paths exist or named **new** | named new (mocked helper) |
| Push strategy + batch push phases explicit | yes |
| No TBD / no open options in decisions table | yes |
