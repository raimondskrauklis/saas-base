# Execution peer review — pass 1

**Plan folder:** `docs/mini-saas/`  
**Date:** 2026-09-20  
**Scope:** full (P0–P6)  
**Reviewer:** execution-peer-review skill  
**Baseline:** [MINI_SAAS_FINDINGS.md](../../MINI_SAAS_FINDINGS.md), [MINI_SAAS_GENERAL_PLAN.md](../../MINI_SAAS_GENERAL_PLAN.md), codebase paths cited below

## Verdict

Review complete: 7 files, 0 critical, 2 high, 6 medium/low.  
**BLOCK phase-execution:** yes — P4 drop-pending gate cannot fail; P6.4 merge/tag is scheduled before the batch WHILE.

## Prior pass

None — first execution peer review.


## Per-phase findings

## P0 — `docs/mini-saas/MINI_SAAS_P0_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| — | — | **no gaps found.** |

**Verified OK:** (1) Q10/Q17 + P0.0 slug match general plan; `ADMIN*` Out. (2) 3 subphases; `api.test.ts` named **new**; `test_keycloak_provisioning.py` exists (no `apply_keycloak_user_disabled` yet — add tests). (3) Push `local`; Bugbot REPEAT inlined; FE + backend gates; omit `rule_packs` matches `manifest.review_context.rule_packs: null`.

## P1 — `docs/mini-saas/MINI_SAAS_P1_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution contract | P1.1 Deliverable runs `tests/unit/test_keycloak_admin.py -k token` but that file is listed **new** only on P1.2. Either add the test path to P1.1 Files or move the token pytest into P1.2. |
| medium | findings / runbook | `redirect_uri` is locked as `app_public_url` or `http://localhost:5173`. MAILGUN_SETUP and KEYCLOAK_SETUP use `{base}/login`. `email_dispatch.py:26` only supplies the **base**; it then appends a path. Lock `redirect_uri = (app_public_url or "http://localhost:5173").rstrip("/") + "/login"` so execute-actions return to the SPA. |

**Verified OK:** (1) `keycloak_frontend_client_id` default `app-web` (`config.py:141`); `app_public_url` exists (`:140`). Helper path `integrations/` matches general plan; example client is `_example_http_client.py.example`. (2) 4 subphases; test file named new; first-push tail skips Revy drain; no create/delete KC users. (3) Settings names are real — not invented.

## P2 — `docs/mini-saas/MINI_SAAS_P2_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| — | — | **no gaps found.** |

**Verified OK:** (1) Q3/Q4/Q7/Q11/Q12 locked; `/pending` kept; KPI schema only. Copy target `admin_workspaces.py` list + `CursorParams`. (2) 4 subphases; `admin_users.py` + `tests/api/test_admin_users.py` named new (api dir today is only `test_workspaces.py` / `test_me.py`). `test_admin_kpis.py` `side_effect=[8, 2, 5, 50, 3]` needs one extra scalar. (3) Push `local`; no FE (correct).

## P3 — `docs/mini-saas/MINI_SAAS_P3_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | codebase | Today `admin/users.py:49` and `:65` `session.commit()` after approve/reject (same as workspace suspend). P3.1 wrapper also **commits**. Lock a single owner: Q22 wrapper does mutate → helper → commit → restore `enabled` on commit-fail; routers must not commit again (unlike `admin_workspaces.py`). |
| medium | general plan | Approve combines status dual-write **and** `VERIFY_EMAIL`. Q22 restore is only commit-fail after `enabled`. If helper PUTs `enabled=true` then execute-actions 4xx, DB rolls back (row stays pending) but KC stays enabled. Lock: restore previous `enabled` (then raise) if `VERIFY_EMAIL` fails after a successful PUT, or run `VERIFY_EMAIL` before `enabled`. |
| medium | execution contract | P3.3 Deliverable and the phase gate run `tests/unit/test_users_service.py` — that file is ME profile (`update_me`), **not** approve/reject. Approve tests are `test_admin_users.py`. Drop the wrong path or put new coverage in `test_admin_users.py` only. |

**Verified OK:** (1) Q22/Q5/Q8/Q13/Q15 match general plan; password-reset route named; live SMTP Out; 4 subphases. (2) `test_users_service.py` exists (wrong for this gate). `actor_user_id` exists on `CurrentUser`. (3) Push `local`.

## P4 — `docs/mini-saas/MINI_SAAS_P4_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| high | execution contract | Drop-pending verify is `rg … && exit 1 \|\| true`. `\|\| true` makes the command always succeed, whether leftovers exist or not. Remove `\|\| true` (fail the gate when `rg` matches). Same line is in P4.4 Deliverable and the phase gate. |

**Verified OK:** (1) Q4 drop, Q7 detail, Q11 card match general plan; copy `WorkspacesPage` + existing `ImpersonationStartModal` (`WorkspaceDetailPage.tsx`). (2) 4 subphases; `UserDetailPage.tsx` named new; `AdminUsersPage.test.tsx` / `AdminDashboardPage.test.tsx` exist and still mock `fetchPendingUsers`. (3) Push `batch` with Revy fetch-before-push + WHILE; FE lint+test in the gate.

## P5 — `docs/mini-saas/MINI_SAAS_P5_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| — | — | **no gaps found.** |

**Verified OK:** (1) Q1–Q22 stay locked; no irbene_gate / tag (those are P6). (2) 4 subphases; fetch-all comments; empty-commit skip; no-op push skip. (3) Push `batch` + WHILE; Next → P6.

## P6 — `docs/mini-saas/MINI_SAAS_P6_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| high | execution contract | P6.4 (merge PR → tag `saas-base-v3` on `main`) is a **subphase before** the LOOP ship gate. Agents run every Deliverable then the gate: they would merge/tag **before** the P6 closeout commit, Revy fetch, and WHILE. Ship-gate **8b** already does merge/tag after WHILE — keep merge/tag **only** there. P6.4 should be docs-only (SHA placeholder) or deleted. |
| low | execution contract | Phase gate `rg -n 'No in-app user directory' docs/saas-base/waves/SAAS_BASE_W5_EXECUTION.md` still matches after a “superseded” rewrite of the same sentence. That is a presence check, not a supersede check. Optional: `rg -n 'superseded' docs/saas-base/waves/SAAS_BASE_W5_EXECUTION.md`. |

**Verified OK:** (1) Q20/Q21 Out (no irbene_gate copy, no retag v2, no `WEBHOOK_EVENTS_TAKEN` change). Gap table → fix-now → doc grep → `active_program` null. (2) 4 subphases; both review-context files; W5 paths still contain the lock (`SAAS_BASE_W5_EXECUTION.md:14`). (3) Push `batch` + WHILE; Next none.

## Rollup (critical / high)

| Severity | Phase | Title |
|:---|:---|:---|
| high | P4 | Drop-pending `rg` gate swallowed by `\|\| true` |
| high | P6 | Merge/tag subphase runs before batch WHILE |

## Open questions

None — fix agent can apply both highs without a product decision.

## Suggested edits (fix agent)

1. **P4.4 + P4 phase gate:** `rg -n 'users/pending|list_pending_users|fetchPendingUsers' backend frontend; test $? -eq 1` (fail if matches). No `\|\| true`.
2. **P6:** remove merge/tag from P6.4 (or make P6.4 “SHA row placeholder only”). Keep merge/tag exclusively in ship-gate 8b after WHILE idle.
3. **P3 (medium):** one commit owner for Q22; restore `enabled` if `VERIFY_EMAIL` fails after PUT; drop `test_users_service.py` from the P3 gate.
4. **P1 (medium):** add test file to P1.1 Files; lock `redirect_uri` to `{base}/login`.

