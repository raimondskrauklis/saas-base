# .gitleaks.toml
# Revy — extends gitleaks defaults. Manual: gitleaks detect --source . --config .gitleaks.toml

title = "saas-base"

[extend]
useDefault = true

[[allowlists]]
description = "Frontend build artefacts"
paths = [
  '''(?i)frontend/dist/''',
  '''(?i)frontend/node_modules/''',
]