# deploy/keycloak/config/.env
# RUNTIME — read by docker compose from this directory (env_file: .env)
# chmod 600
#
# Build-time (KC_DB, KC_HEALTH_ENABLED, KC_METRICS_ENABLED) is in Dockerfile.
# Rebuild image if you change those: docker compose build --no-cache
#
# Issuer: https://auth.revy.createit.digital/realms/revy
# Backend: KEYCLOAK_URL=http://keycloak:8080 (compose alias on revy-net)

KC_BOOTSTRAP_ADMIN_USERNAME=admin
KC_BOOTSTRAP_ADMIN_PASSWORD=CHANGE_ME

KC_DB=postgres
KC_DB_URL=jdbc:postgresql://db-pgsql-fra1-18565-do-user-14700337-0.h.db.ondigitalocean.com:25060/revy-sec-staging?sslmode=require
KC_DB_USERNAME=sec-user-staging
KC_DB_PASSWORD=CHANGE_ME

KC_HOSTNAME=https://auth.revy.createit.digital
KC_HOSTNAME_STRICT=true
KC_HTTP_ENABLED=true
KC_PROXY_HEADERS=xforwarded
KC_PROXY_TRUSTED_ADDRESSES=127.0.0.1

KC_LOG_LEVEL=INFO

# vymalo/keycloak-webhook 0.10.0-rc.1 — identity events to the API (internal docker network)
# Prod: http://backend:8000/api/v1/webhooks/keycloak
# Dev (API on host): http://host.docker.internal:8000/api/v1/webhooks/keycloak
# Optional for local Mode A (JIT is enough). Required for a deployed Keycloak listener.
WEBHOOK_HTTP_BASE_PATH=http://backend:8000/api/v1/webhooks/keycloak
WEBHOOK_HTTP_AUTH_USERNAME=app
WEBHOOK_HTTP_AUTH_PASSWORD=CHANGE_ME_SAME_AS_KEYCLOAK_WEBHOOK_SECRET
WEBHOOK_EVENTS_TAKEN=REGISTER,IDENTITY_PROVIDER_FIRST_LOGIN,IDENTITY_PROVIDER_LINK,UPDATE_PROFILE,UPDATE_EMAIL,DELETE_ACCOUNT
