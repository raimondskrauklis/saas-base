# backend/docker-compose.yml
# Local infrastructure only — Redis for Celery broker/cache.
# PostgreSQL: DigitalOcean managed (ENVIRONMENT selects DEV_/TEST_/STAGING_/PRODUCTION_DATABASE_URL).

services:
  redis:
    image: redis:7-alpine
    ports:
      - "127.0.0.1:6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
    restart: unless-stopped
